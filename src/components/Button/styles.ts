import { styled } from '../../styles'

export const ButtonContainer = styled('button', {
  backgroundColor: '$primary',
  border: 'none',
  borderRadius: 8,
  height: 80,

  fontSize: '$xl',
  color: '$white',
  cursor: 'pointer',
})
